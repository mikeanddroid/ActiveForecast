<resources>

    <!--
        Base application theme, dependent on API level. This theme is replaced
        by AppBaseTheme from res/values-vXX/styles.xml on newer devices.
    -->
    <style name="AppBaseTheme" parent="Theme.AppCompat.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.
        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">
        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
    </style>
    
    <style name="ToolBarStyle" parent="@style/ThemeOverlay.AppCompat.ActionBar">
        <item name="android:textColorPrimary">@color/white</item>
        <item name="android:homeAsUpIndicator">@drawable/appbar_back_trans</item>
    </style>
    
    <style name="ToolBarStyleRefine" parent="ToolBarStyle">
        <item name="android:actionMenuTextColor">@color/ttdPrimaryColor</item>
        <item name="android:actionMenuTextAppearance">@style/MyMenuTextAppearance</item>
    </style>
    
    <style name="DefaultActionBar" parent="Widget.AppCompat.ActionBar">
        <item name="theme">@style/ToolBarStyle</item>
    </style>

    <style name="MyMenuTextAppearance" parent="@style/Base.TextAppearance.AppCompat.Button">
        <item name="android:textAllCaps">false</item>
    </style>
    
</resources>
